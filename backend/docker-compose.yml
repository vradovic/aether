services:
  postgres:
    image: postgres:18-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 3s
      timeout: 3s
      retries: 5

  migrate:
    image: gomicro/goose
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      GOOSE_DRIVER: postgres
      GOOSE_DBSTRING: "host=postgres port=5432 user=${POSTGRES_USER} password=${POSTGRES_PASSWORD} dbname=${POSTGRES_DB} sslmode=disable"
    volumes:
      - ./sql/migrations:/migrations:ro
      - ./sql/seeds:/seeds:ro
    entrypoint: ["sh", "-c"]
    command:
      - |
        goose -dir /migrations up
        goose -dir /seeds -no-versioning up

  nats:
    image: nats:2.11-alpine
    restart: unless-stopped
    command: ["-js", "-m", "8222"]
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8222/healthz"]
      interval: 3s
      timeout: 3s
      retries: 5

  nats-box:
    image: natsio/nats-box:latest
    restart: "no"
    depends_on:
      nats:
        condition: service_healthy
    volumes:
      - ./deployments/nats-stream.json:/config/nats-stream.json:ro
      - ./deployments/nats-consumer.json:/config/nats-consumer.json:ro
    entrypoint:
      - sh
      - -c
      - |
        nats stream info MESSAGES -s nats:4222 >/dev/null 2>&1 || nats stream add --config /config/nats-stream.json -s nats:4222
        nats consumer info MESSAGES message-service -s nats:4222 >/dev/null 2>&1 || nats consumer add MESSAGES --config /config/nats-consumer.json -s nats:4222

  scylla:
    image: scylladb/scylla:6.2
    restart: unless-stopped
    command: --smp 1 --memory 1G --developer-mode 1
    volumes:
      - ../scylla:/scylla:ro
    healthcheck:
      test: ["CMD-SHELL", "cqlsh -e 'describe cluster'"]
      interval: 5s
      timeout: 5s
      retries: 5

  scylla-init:
    image: scylladb/scylla:6.2
    depends_on:
      scylla:
        condition: service_healthy
    volumes:
      - ./scylla/init.cql:/init.cql
    entrypoint: ["cqlsh", "scylla", "-f", "/init.cql"]

  realtime-service:
    build:
      context: .
      dockerfile: cmd/realtime/Dockerfile
    ports:
      - "8080-8089:8080"
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      SERVER_ADDRESS: 0.0.0.0:8080
      DB_ADDRESS: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable
      NATS_ADDRESS: nats:4222
      NATS_SUBJECT: message
      JWT_SIGNING_KEY: ${JWT_SIGNING_KEY}
    depends_on:
      migrate:
        condition: service_completed_successfully
      nats-box:
        condition: service_completed_successfully
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--no-verbose",
          "--tries=1",
          "--spider",
          "http://localhost:8080/healthz",
        ]
      interval: 3s
      timeout: 3s
      retries: 5

  api-service:
    build:
      context: .
      dockerfile: cmd/api/Dockerfile
    restart: unless-stopped
    environment:
      SERVER_ADDRESS: 0.0.0.0:8081
      DB_ADDRESS: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable
      SCYLLA_HOSTS: scylla:9042
      SCYLLA_KEYSPACE: aether
      JWT_SIGNING_KEY: ${JWT_SIGNING_KEY}
    depends_on:
      migrate:
        condition: service_completed_successfully
      scylla-init:
        condition: service_completed_successfully

  nginx:
    image: nginx:1.27-alpine
    restart: unless-stopped
    ports:
      - "8000:80"
    volumes:
      - ./deployments/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      api-service:
        condition: service_started
      realtime-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost/healthz"]
      interval: 3s
      timeout: 3s
      retries: 5

  worker-service:
    build:
      context: .
      dockerfile: cmd/worker/Dockerfile
    restart: unless-stopped
    environment:
      SERVER_ADDRESS: 0.0.0.0:8079
      NATS_ADDRESS: nats:4222
      NATS_STREAM: MESSAGES
      NATS_DURABLE: message-service
      NATS_SUBJECT: messages.unprocessed.*
      SCYLLA_HOSTS: scylla:9042
      SCYLLA_KEYSPACE: aether
    depends_on:
      nats-box:
        condition: service_completed_successfully
      scylla-init:
        condition: service_completed_successfully

  prometheus:
    image: prom/prometheus:latest
    user: root
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - "9090:9090"
    depends_on:
      realtime-service:
        condition: service_healthy
